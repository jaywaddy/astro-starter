---
import GlobalLayout from "../layouts/GlobalLayout.astro";
import DATA from "../content/globalData";

const { brand } = DATA;

const seo_data = {
	title: "Home",
	//
};
---

<GlobalLayout data={seo_data}>
	<h1>{brand.name}</h1>
</GlobalLayout>
