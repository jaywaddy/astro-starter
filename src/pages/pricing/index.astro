---
import Container from "@layouts/Container.astro";
import GlobalGrid from "@layouts/GlobalGrid.astro";
import GlobalLayout from "@layouts/GlobalLayout.astro";
import { frontmatter } from "./pricing.md";
import globalConfig from "@lib/config";
import Tabs from "@components/ui/Tabs.astro";

const { setWarning } = globalConfig.utils ?? {};

if (!globalConfig) {
	setWarning("Contact - index.astro", "globalConfig");
	return null;
}

const { title, description } = frontmatter ?? {};

const plans = globalConfig.services;
---

<GlobalLayout {...frontmatter}>
	<GlobalGrid class="gap-y-8">
		<Container class="flex flex-col items-center gap-8" layout="content">
			<div class="grid max-w-[60ch] gap-4 text-center">
				<h1>{title}</h1>
				<p class="lead">{description}</p>
			</div>
			<Tabs id="pricing-tabs" tabs={["Monthly", ["Annually", true], "Single/OTP"]} />
		</Container>
	</GlobalGrid>
</GlobalLayout>
